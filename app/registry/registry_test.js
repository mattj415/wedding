'use strict';

describe('myApp.registry module', function() {

  beforeEach(module('myApp.registry'));

  describe('registry controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var registryCtrl = $controller('registryCtrl');
      expect(registryCtrl).toBeDefined();
    }));

  });
});