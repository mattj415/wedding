'use strict';

describe('myApp.event_info module', function() {

  beforeEach(module('myApp.event_info'));

  describe('event_info controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var event_infoCtrl = $controller('event_infoCtrl');
      expect(event_infoCtrl).toBeDefined();
    }));

  });
});